'use strict';

/**
 * level-question-composition router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::level-question-composition.level-question-composition');
