'use strict';

/**
 * level-question-composition controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::level-question-composition.level-question-composition');
