'use strict';

/**
 * world-question-composition controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::world-question-composition.world-question-composition');
