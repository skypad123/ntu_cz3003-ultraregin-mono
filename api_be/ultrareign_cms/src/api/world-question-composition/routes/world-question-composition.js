'use strict';

/**
 * world-question-composition router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::world-question-composition.world-question-composition');
