'use strict';

/**
 * world-question-composition service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::world-question-composition.world-question-composition');
